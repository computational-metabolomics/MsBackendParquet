% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mzml-to-parquet.R
\name{mzMLToParquet}
\alias{mzMLToParquet}
\title{Convert mzML (or related) files into a Parquet dataset}
\usage{
mzMLToParquet(
    files,
    path,
    partitioning = character(),
    compression = "snappy",
    chunksize = 10L,
    overwrite = FALSE,
    BPPARAM = BiocParallel::SerialParam(),
    engine = c("spectra", "mzr"),
    batch_size = 1000L,
    verbose = TRUE)
}
\arguments{
\item{files}{\code{character} with the paths to the input mzML, mzXML
or netCDF files. Files must exist on disk and have a recognised
extension (\code{.mzML}, \code{.mzXML}, \code{.cdf}, \code{.nc};
case-insensitive).}

\item{path}{\code{character(1)} with the destination directory for the
Parquet dataset.}

\item{partitioning}{\code{character} with the spectra variables to use
as Hive partitioning columns (e.g. \code{"dataOrigin"}).}

\item{compression}{\code{character(1)} Parquet compression codec.}

\item{chunksize}{\code{integer(1)} number of input files processed
per iteration.}

\item{overwrite}{\code{logical(1)} whether to remove an existing
dataset at \code{path} before writing.}

\item{BPPARAM}{Parallel processing setup forwarded to the underlying
\code{Spectra} reads (only honoured when \code{engine = "spectra"}).}

\item{engine}{\code{character(1)} import engine, forwarded to
\code{\link{createMsBackendParquetDataset}}. \code{"spectra"} uses
\code{Spectra}/\code{MsBackendMzR}; \code{"mzr"} streams directly
from mzML via \code{mzR} for bounded-memory imports.}

\item{batch_size}{\code{integer(1)} spectra per write batch when
\code{engine = "mzr"}.}

\item{verbose}{\code{logical(1)} whether to print a short summary of
the resulting dataset.}
}
\value{
Invisibly returns an initialised \code{\link{MsBackendParquet}}
instance pointing at the new dataset.
}
\description{
Thin convenience wrapper around
\code{\link{createMsBackendParquetDataset}} for the common use-case of
importing mzML / mzXML / netCDF files into a new on-disk Parquet
dataset that can be read by \code{\link{MsBackendParquet}}.
}
\details{
Performs basic input validation (file existence and recognised
extension), optionally removes a stale destination directory, and
returns an initialised backend. The actual import is delegated to
\code{createMsBackendParquetDataset()} using \code{MsBackendMzR()} as
the reader.
}
\examples{
\dontrun{
files <- c("sample-1.mzML", "sample-2.mzML")
be <- mzMLToParquet(files, path = tempfile(),
                    partitioning = "dataOrigin")
library(Spectra)
sps <- Spectra(be)
}
}
\seealso{
\code{\link{createMsBackendParquetDataset}},
\code{\link{MsBackendParquet}}
}
\author{
Ossama Edbali
}
