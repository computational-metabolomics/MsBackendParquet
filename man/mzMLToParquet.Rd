% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MsBackendParquet-creators.R
\name{mzMLToParquet}
\alias{mzMLToParquet}
\title{Convert mzML (or related) files into a Parquet dataset}
\usage{
mzMLToParquet(
  files,
  path,
  partitioning = character(),
  compression = .DEFAULT_COMPRESSION,
  chunksize = 10L,
  overwrite = FALSE,
  BPPARAM = BiocParallel::SerialParam(),
  engine = c("spectra", "mzr"),
  batch_size = 1000L,
  row_group_size = .DEFAULT_ROW_GROUP_SIZE,
  verbose = TRUE
)
}
\arguments{
\item{files}{\code{character} with the paths to the input mzML, mzXML or
netCDF files. Files must exist on disk and have a recognised
extension (\code{.mzML}, \code{.mzXML}, \code{.cdf}, \code{.nc}; case-insensitive).}

\item{path}{\code{character(1)} with the destination directory for the
Parquet dataset. The directory must not already contain a
\code{MsBackendParquet} dataset, unless \code{overwrite = TRUE}.}

\item{partitioning}{\code{character} with the spectra variables to use
as Hive partitioning columns (e.g. \code{"dataOrigin"}). Empty
(default) writes a single un-partitioned set of files.}

\item{compression}{\code{character(1)} Parquet compression codec, passed
to \code{\link[arrow:write_parquet]{arrow::write_parquet()}} / \code{\link[arrow:write_dataset]{arrow::write_dataset()}}. Defaults
to \code{"snappy"}.}

\item{chunksize}{\code{integer(1)} number of input files processed per
iteration. Higher values trade memory for throughput.}

\item{overwrite}{\code{logical(1)} whether to remove an existing dataset
at \code{path} before writing. Defaults to \code{FALSE}.}

\item{BPPARAM}{Parallel processing setup forwarded to the
underlying \code{\link[Spectra:Spectra]{Spectra::Spectra()}} / \code{MsBackendMzR} reads (only
honoured when \code{engine = "spectra"}).}

\item{engine}{\code{character(1)} import engine, passed through to
\code{\link[=createMsBackendParquetDataset]{createMsBackendParquetDataset()}}. \code{"spectra"} (default) goes
through \code{\link[Spectra:Spectra]{Spectra::Spectra()}} and \code{MsBackendMzR()}; \code{"mzr"}
streams batches of spectra directly from mzML via
\code{\link[mzR:openMSfile]{mzR::openMSfile()}} for bounded-memory imports of large
files. \code{mzR} must be installed for \code{"mzr"}.}

\item{batch_size}{\code{integer(1)} number of spectra per write batch
when \code{engine = "mzr"}. Ignored otherwise.}

\item{row_group_size}{\code{integer(1)} number of spectra per Parquet
row group. See \code{\link[=createMsBackendParquetDataset]{createMsBackendParquetDataset()}} for details.
Defaults to 250.}

\item{verbose}{\code{logical(1)} whether to print a short summary of the
resulting dataset.}
}
\value{
Invisibly returns an initialised \code{\link[=MsBackendParquet]{MsBackendParquet()}}
instance pointing at the new dataset.
}
\description{
\code{mzMLToParquet()} is a thin convenience wrapper around
\code{\link[=createMsBackendParquetDataset]{createMsBackendParquetDataset()}} tailored to the most common
use-case: importing one or more mass spectrometry data files
(mzML, mzXML, netCDF) from disk into a new Apache Parquet dataset
that can be read back by \code{\link[=MsBackendParquet]{MsBackendParquet()}}.

Compared to calling \code{createMsBackendParquetDataset()} directly, this
function performs basic input validation (existence and recognised
file extension), optionally tears down a stale destination
directory, prints a short summary of what was written and returns
an initialised \code{MsBackendParquet} instance ready to be wrapped in a
\code{\link[Spectra:Spectra]{Spectra::Spectra()}} object.
}
\examples{
\dontrun{
files <- c("sample-1.mzML", "sample-2.mzML")
be <- mzMLToParquet(files, path = tempfile(),
                    partitioning = "dataOrigin")
library(Spectra)
sps <- Spectra(be)
}
}
\seealso{
\code{\link[=createMsBackendParquetDataset]{createMsBackendParquetDataset()}} for the full API,
\code{\link[=MsBackendParquet]{MsBackendParquet()}} for the backend class.
}
