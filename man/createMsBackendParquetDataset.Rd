% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/createMsBackendParquetDataset.R
\name{createMsBackendParquetDataset}
\alias{createMsBackendParquetDataset}
\title{Create a Parquet dataset from raw MS data or a \code{Spectra} object}
\usage{
createMsBackendParquetDataset(
    path = character(),
    x = character(),
    data,
    backend = Spectra::MsBackendMzR(),
    chunksize = 10L,
    partitioning = character(),
    compression = "snappy",
    BPPARAM = BiocParallel::SerialParam(),
    engine = c("spectra", "mzr"),
    batch_size = 1000L)
}
\arguments{
\item{path}{\code{character(1)} with the path to the (new) dataset
directory.}

\item{x}{\code{character} with the file paths to the raw MS data files to
import. Ignored if \code{data} is provided.}

\item{data}{Optional \code{DataFrame} (as returned by
\code{\link[Spectra]{spectraData}}) holding the full spectra data to
write to disk. Must contain \code{mz} and \code{intensity} columns.}

\item{backend}{MS backend used to read the raw input files.}

\item{chunksize}{\code{integer(1)} with the number of input files
processed per iteration.}

\item{partitioning}{\code{character} with the spectra variables to use
as Hive partitioning columns.}

\item{compression}{\code{character(1)} with the Parquet compression
codec.}

\item{BPPARAM}{Parallel processing setup (only honoured when
\code{engine = "spectra"}).}

\item{engine}{\code{character(1)} import engine. \code{"spectra"}
(default) goes through \code{\link[Spectra]{Spectra}} and
\code{MsBackendMzR()}. \code{"mzr"} streams batches of spectra
directly from mzML / mzXML / netCDF through
\code{\link[mzR]{openMSfile}} into Apache Arrow's incremental
Parquet writer for bounded-memory imports. Requires \code{mzR}.}

\item{batch_size}{\code{integer(1)} spectra per row group / write
call when \code{engine = "mzr"}.}
}
\value{
Invisibly returns the (normalised) \code{path}.
}
\description{
Import raw MS data files (mzML, mzXML or netCDF) - or an in-memory
\code{DataFrame} - into a new on-disk Apache Parquet dataset that can be
read back by \code{\link{MsBackendParquet}}.
}
\details{
Spectra metadata are written alongside the \code{mz} and \code{intensity} peaks
data as Parquet \code{list<double>} columns. When \code{partitioning} is
non-empty, Apache Arrow writes a Hive-partitioned dataset which
enables partition pruning on the supplied variables.
}
\examples{
library(MsBackendParquet)

sd <- S4Vectors::DataFrame(
    msLevel = c(1L, 2L), rtime = c(1.0, 2.0),
    dataOrigin = c("a", "b"))
sd$mz <- IRanges::NumericList(c(100, 110), c(101, 111),
                              compress = FALSE)
sd$intensity <- IRanges::NumericList(c(10, 20), c(11, 21),
                                     compress = FALSE)

path <- tempfile()
createMsBackendParquetDataset(path = path, data = sd)
be <- backendInitialize(MsBackendParquet(), path = path)
be
}
\author{
Ossama Edbali
}
