% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MsBackendParquet-creators.R
\name{createMsBackendParquetDataset}
\alias{createMsBackendParquetDataset}
\title{Create a Parquet dataset from raw MS data or a \code{Spectra} object}
\usage{
createMsBackendParquetDataset(
  path = character(),
  x = character(),
  data,
  backend = MsBackendMzR(),
  chunksize = 10L,
  partitioning = character(),
  compression = .DEFAULT_COMPRESSION,
  BPPARAM = BiocParallel::SerialParam(),
  engine = c("spectra", "mzr"),
  batch_size = 1000L,
  row_group_size = .DEFAULT_ROW_GROUP_SIZE
)
}
\arguments{
\item{path}{\code{character(1)} with the path to the dataset directory.
The directory must not already contain a \code{MsBackendParquet}
dataset (use a fresh path for new datasets).}

\item{x}{\code{character} with the names of the raw MS data files to
import. Ignored if \code{data} is supplied.}

\item{data}{Optional \code{DataFrame} with the full spectra data to be
written. Must include the \code{mz} and \code{intensity} peaks variables.
When supplied, \code{x} and \code{backend} are ignored.}

\item{backend}{MS backend used to read the raw input files. Defaults
to \code{MsBackendMzR()}.}

\item{chunksize}{\code{integer(1)} defining the number of input files
processed per iteration.}

\item{partitioning}{\code{character} with the names of the spectra
variables to use as Hive partitioning columns. Use an empty
vector (the default) to write a single un-partitioned set of
files.}

\item{compression}{\code{character(1)} with the Parquet compression codec
to use. Defaults to \code{"snappy"}. See
\code{\link[arrow:write_parquet]{arrow::write_parquet()}} for supported values.}

\item{BPPARAM}{Parallel processing setup used for reading the raw
input files (only honoured when \code{engine = "spectra"}). Defaults
to \code{\link[BiocParallel:SerialParam]{BiocParallel::SerialParam()}}.}

\item{engine}{\code{character(1)} selecting the import engine. The
default \code{"spectra"} reads each chunk of files through a
\code{\link[Spectra:Spectra]{Spectra::Spectra()}} object (high throughput, supports
\code{BPPARAM}). \code{"mzr"} streams spectra directly from mzML / mzXML
/ netCDF through \code{\link[mzR:openMSfile]{mzR::openMSfile()}} into Apache Arrow's
incremental Parquet writer, bounding memory to roughly
\code{batch_size} spectra of peaks at a time. The streaming engine
requires only raw input files (no \code{data} argument) and ignores
\code{chunksize} / \code{BPPARAM}. \code{mzR} must be installed.}

\item{batch_size}{\code{integer(1)} number of spectra per write call when
\code{engine = "mzr"}. Larger values amortise write overhead at the
cost of higher transient memory; smaller values keep memory
tighter. Independent of \code{row_group_size}. Ignored for
\code{engine = "spectra"}.}

\item{row_group_size}{\code{integer(1)} number of spectra per Parquet
row group. Smaller row groups give finer-grained pruning for
narrow range queries (e.g. retention time) at the cost of more
file metadata; larger groups compress better and reduce per-group
scan setup. Defaults to 250.}
}
\value{
Invisibly returns \code{path} (with any tilde expanded).
}
\description{
\code{createMsBackendParquetDataset()} imports raw MS data from one or more
input files (mzML, mzXML or netCDF) into a new on-disk Parquet dataset
that can be used by \code{\link[=MsBackendParquet]{MsBackendParquet()}}. Alternatively, an existing
\code{DataFrame} (such as the output of \code{\link[Spectra:spectraData]{Spectra::spectraData()}}) can be
written to disk directly via the \code{data} argument.
}
\details{
The function writes a single Parquet dataset (under
\verb{<path>/spectra}) containing the spectra metadata plus the m/z and
intensity values as Parquet \verb{list<double>} columns.

Spectra metadata that are entirely \code{NA} (and not part of the Spectra
core variables) are dropped before writing to keep the on-disk
dataset compact.

Each source file becomes its own run, written to
\verb{<path>/spectra/run_id=<id>/}, so a filter that selects whole files
already prunes at the directory level. If \code{partitioning} is supplied
(e.g. \code{partitioning = "msLevel"}), Apache Arrow adds a further Hive level
below the run, which typically enables Arrow's partition pruning when
filtering and can substantially speed up access to large datasets.
\code{"dataOrigin"} is not useful as a key, since it is constant within a run;
it is dropped with a warning.
}
\examples{
library(Spectra)

## Spectra data can be written to a Parquet dataset directly, without
## going through raw files: any `DataFrame` with `mz` and `intensity`
## peaks variables will do.
sd <- S4Vectors::DataFrame(
    msLevel = c(1L, 1L, 2L),
    rtime = c(1.0, 2.0, 3.0),
    dataOrigin = c("file-a", "file-a", "file-b"))
sd$mz <- IRanges::NumericList(c(100, 110), c(101, 111),
                              c(102, 112), compress = FALSE)
sd$intensity <- IRanges::NumericList(c(10, 20), c(11, 21),
                                     c(12, 22), compress = FALSE)

## Each `dataOrigin` becomes its own run directory, so a filter on it
## skips whole files rather than scanning them. `msLevel` adds a second
## Hive level below the run.
path <- tempfile()
createMsBackendParquetDataset(path = path, data = sd,
                              partitioning = "msLevel")

be <- backendInitialize(MsBackendParquet(), path = path)
be
spectraVariables(be)
peaksData(be)

## Raw files are imported with `x` instead of `data`.
if (requireNamespace("mzR", quietly = TRUE)) {
    fl <- system.file("extdata", "demo.mzML",
                      package = "MsBackendParquet")
    from_files <- tempfile()
    createMsBackendParquetDataset(path = from_files, x = fl)
    print(backendInitialize(MsBackendParquet(), path = from_files))
}
}
\seealso{
\code{\link[=mzMLToParquet]{mzMLToParquet()}} for a one-call wrapper over raw mzML files,
\code{\link[=MsBackendParquet]{MsBackendParquet()}} to open the resulting dataset.
}
