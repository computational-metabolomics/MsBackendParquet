% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mzstack-manifest.R
\name{mzstack-manifest}
\alias{mzstack-manifest}
\alias{newManifest}
\alias{readManifest}
\alias{writeManifest}
\alias{manifestRuns}
\alias{invalidateDatasetCache}
\title{Read and write an mzStack manifest}
\usage{
newManifest()

readManifest(path)

writeManifest(path, manifest, bump = TRUE)

manifestRuns(path)

invalidateDatasetCache(path)
}
\arguments{
\item{path}{\code{character(1)}, the dataset directory, or an
\code{MsBackendParquet} over it.}

\item{manifest}{\code{list}, as returned by \code{readManifest()} and modified.}

\item{bump}{\code{logical(1)}, whether to increment \code{generation}. Every change
to a dataset's content must, so leave this \code{TRUE} unless the change is
one the specification exempts.}
}
\value{
\code{newManifest()} and \code{readManifest()} return a \code{list}. \code{writeManifest()} returns the
written manifest invisibly, with its new \code{generation}.
\code{manifestRuns()} returns a \code{data.frame}. \code{invalidateDatasetCache()}
returns \code{NULL} invisibly.
}
\description{
Every mzStack dataset is described by a manifest, \code{mzStack.json}, at the
root of its directory. These functions give packages layered on
MsBackendParquet, such as a results layer that adds its own top-level keys,
access to it without re-implementing the format's rules:
\itemize{
\item \code{newManifest()} returns the manifest of an empty dataset: no runs, at
generation 1.
\item \code{readManifest()} parses and validates the manifest. Keys this package
does not interpret are returned exactly as parsed, arrays as \code{list}s, so
they survive a read/write cycle unchanged.
\item \code{writeManifest()} replaces the manifest atomically: the new version is
written to a temporary file and renamed into place, so a reader never
observes a half-written manifest. It increments \code{generation}, refuses to
overwrite a manifest another writer has committed since \code{manifest} was
read, and drops every cache this package holds for the dataset.
Numbers are written so that they read back exactly. Where \code{path} holds
no manifest yet, the directory is created if needed and \code{manifest} is
written as the dataset's first, without incrementing \code{generation}.
\item \code{manifestRuns()} returns the run entries as a \code{data.frame}, one row per
run: \code{run_id}, \code{kind}, \code{path}, \code{n_spectra}, \code{uid_base}, \code{ingested_at},
\code{layout}, \code{profile}, \code{centroid} and \code{source}. The tuple
\verb{(run_id, uid_base, n_spectra, ingested_at)} is the run's fingerprint:
while it is unchanged, the run's \code{spectrum_id_} values address the same
spectra.
\item \code{invalidateDatasetCache()} drops the parsed manifest, the DuckDB view,
the metadata cache and the sample metadata this process holds for a
dataset. \code{writeManifest()} calls it; call it directly after changing a
dataset's files by other means. A manifest replaced by another process is
detected without it.
}

The manifest must be written last, after every file it declares is
durably on disk: its atomic replacement is the commit point of any write.
}
\examples{
fl <- system.file("extdata", "QC01.mzpeak", package = "MsBackendParquet")
ds <- createMzPeakDataset(fl, file.path(tempdir(), "manifest-example"))
m <- readManifest(ds)
m$generation
manifestRuns(ds)[, c("run_id", "uid_base", "n_spectra", "ingested_at")]

## Add a key of another layer. It survives this package's own writes.
m$role <- "study"
m <- writeManifest(ds, m)
m$generation
}
\author{
Ossama Edbali
}
