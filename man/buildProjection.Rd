% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projections.R
\name{buildProjection}
\alias{buildProjection}
\alias{dropProjection}
\title{Build or drop signal projections}
\usage{
buildProjection(path, type = "mzsorted", runs = NULL, verbose = TRUE)

dropProjection(path, type = "mzsorted", runs = NULL, verbose = TRUE)
}
\arguments{
\item{path}{\code{character(1)} with the dataset directory.}

\item{type}{\code{character(1)} with the projection type. Currently only
\code{"mzsorted"} is available.}

\item{runs}{\code{character} with the run identifiers to build (or drop). The
default, \code{NULL}, means every run in the dataset.}

\item{verbose}{\code{logical(1)} whether to report progress.}
}
\value{
\code{path}, invisibly.
}
\description{
\code{buildProjection()} writes an m/z-ordered copy of a dataset's signal
data, which makes queries that search for peaks at a given m/z across
many runs substantially faster. \code{dropProjection()} deletes it again.
}
\details{
A projection is a \strong{cache}: it is derived from the mzPeak archives, it
can be deleted at any time, and no result depends on it for correctness.
Queries that can use it do; queries that cannot fall back to reading the
archives directly.

The cost is disk space: a projection is a second copy of the signal data,
of broadly the same size (sorting compresses better, but how much better
depends entirely on the data).
}
\examples{
\dontrun{
buildProjection(path)
filterContainsMz(be, 278.093, ppm = 20)
}
}
