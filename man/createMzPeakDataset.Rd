% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ingest.R
\name{createMzPeakDataset}
\alias{createMzPeakDataset}
\alias{addMzPeakArchives}
\title{Build a queryable dataset from mzPeak archives}
\usage{
createMzPeakDataset(
  archives,
  path,
  link = c("reference", "copy"),
  verbose = TRUE
)

addMzPeakArchives(
  path,
  archives,
  link = c("reference", "copy"),
  verbose = TRUE
)
}
\arguments{
\item{archives}{\code{character} with paths to mzPeak archives. Each may be an
unpacked directory or a \code{.mzpeak} ZIP file. ZIPs are unpacked into
\verb{<path>/runs}.}

\item{path}{\code{character(1)} with the dataset directory. For
\code{createMzPeakDataset()} it must not already hold a dataset.}

\item{link}{\code{character(1)}; \code{"reference"} (the default) records where each
archive is and leaves it there, \code{"copy"} copies it under \verb{<path>/runs}
so the dataset is self-contained.}

\item{verbose}{\code{logical(1)} whether to report progress.}
}
\value{
\code{path}, invisibly.
}
\description{
\code{createMzPeakDataset()} registers one or more
\href{https://github.com/HUPO-PSI/mzPeak}{HUPO-PSI mzPeak} archives as a single
queryable dataset and \code{addMzPeakArchives()} adds more to an existing one.

The archives are \strong{not modified}, and by default not copied: only a small
derived index of the spectrum metadata is written, next to a manifest
recording where each archive lives. Peak data continues to be read from
the archives themselves.
}
\details{
Ingest reads each archive's \code{mzpeak_index.json}, its spectrum metadata
table and its scan / precursor / selected-ion side tables. It does not
read signal data, so the cost is proportional to the number of spectra
rather than to the size of the archives.

Side tables are flattened: a spectrum with several scans contributes the
first, and the number that were present is recorded in \code{n_scans} (and
likewise \code{n_selected_ions}, \code{n_precursors}). The full detail remains in
the archive.

Archives using mzPeak's \emph{chunked} signal layout are rejected; only the
\emph{point} layout is supported.
}
\examples{
\dontrun{
path <- tempfile()
createMzPeakDataset(c("run1.mzpeak", "run2.mzpeak"), path = path)
library(Spectra)
sps <- Spectra(backendInitialize(MsBackendParquet(), path = path))
}
}
\seealso{
\code{\link[=MsBackendParquet]{MsBackendParquet()}} to open the resulting dataset.
}
