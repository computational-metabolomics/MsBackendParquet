% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/projections.R
\name{filterContainsMz}
\alias{filterContainsMz}
\title{Select spectra containing a peak at a given m/z}
\usage{
filterContainsMz(object, mz = numeric(), tolerance = 0, ppm = 20)
}
\arguments{
\item{object}{a \code{\link[=MsBackendParquet]{MsBackendParquet()}} over an mzPeak dataset.}

\item{mz}{\code{numeric} with the m/z values to look for.}

\item{tolerance}{\code{numeric} with the absolute m/z tolerance.}

\item{ppm}{\code{numeric} with the m/z-relative tolerance in parts per million.}
}
\value{
\code{object} subset to the matching spectra, in their original order.
}
\description{
\code{filterContainsMz()} keeps the spectra that hold at least one peak within
a tolerance of any of the given m/z values. Unlike the peak-level filters
in \code{Spectra}, the search happens inside the storage engine, so it does
not read every spectrum's peaks into R.
}
\details{
The query is much faster when the dataset has an \code{mzsorted} projection
(see \code{\link[=buildProjection]{buildProjection()}}); without one it reads the archives directly and
returns the same answer.
}
