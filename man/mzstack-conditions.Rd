% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/conditions.R
\name{mzstack-conditions}
\alias{mzstack-conditions}
\alias{mzstackCondition}
\alias{mzstackError}
\title{Typed mzStack error conditions}
\usage{
mzstackCondition(
  class = .MZSTACK_ERROR_CLASSES,
  ...,
  data = list(),
  call = NULL
)

mzstackError(class = .MZSTACK_ERROR_CLASSES, ..., data = list(), call = NULL)
}
\arguments{
\item{class}{\code{character(1)}, one of the classes above without the
\code{mzstack_} prefix.}

\item{...}{pieces of the message, pasted together as by \code{\link[=stop]{stop()}}.}

\item{data}{named \code{list} of further fields to carry on the condition, for
example the run or source key the error concerns.}

\item{call}{the call to record on the condition. Defaults to none, which
matches the \code{call. = FALSE} convention used throughout the package.}
}
\value{
\code{mzstackError()} does not return; it signals the condition.
\code{mzstackCondition()} returns it unsignalled.
}
\description{
The mzStack specification (mzStack-5 §3) requires an implementation to
distinguish a fixed set of failure classes. The class names and the
mechanism are left to each implementation; the distinctions are not. In R
they are condition classes, so a caller can react to one kind of failure
without matching message text:

\if{html}{\out{<div class="sourceCode r">}}\preformatted{tryCatch(Spectra(backendInitialize(MsBackendParquet(), path = p)),
         mzstack_format = function(e) NULL)
}\if{html}{\out{</div>}}

Every condition inherits from \code{mzstack_error}, then \code{error} and
\code{condition}, so existing \code{tryCatch(..., error = )} handlers keep working.\tabular{ll}{
   Class \tab Raised when \cr
   \code{mzstack_format} \tab the directory is not readable as mzStack: no manifest, another format, an unsupported major version, mixed run kinds \cr
   \code{mzstack_archive} \tab a referenced mzPeak archive cannot be read as its specification requires \cr
   \code{mzstack_study} \tab a repository study cannot be ingested \cr
   \code{mzstack_stale} \tab a projection, index or reference no longer matches the run it was built from \cr
   \code{mzstack_unsupported} \tab a valid request outside what this implementation supports \cr
   \code{mzstack_semantic} \tab a well-formed request that is incoherent \cr
   \code{mzstack_capability} \tab a request for data the dataset does not contain \cr
   \code{mzstack_resource} \tab a request that cannot be completed within a declared bound \cr
}


A \code{mzstack_capability} error and an empty result are different outcomes:
the first says the question could not be asked, the second that nothing
matched.

\code{mzstackError()} is exported so that packages layered on MsBackendParquet
raise the same classes.
}
\examples{
res <- tryCatch(mzstackError("capability", "no such table"),
                mzstack_capability = function(e) conditionMessage(e))
res
}
\author{
Ossama Edbali
}
